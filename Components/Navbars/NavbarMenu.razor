@namespace TablerForNet.Components
@inherits TablerBaseComponent

<div class="navbar-collapse @(!Navbar.IsExpanded ? "collapse":"")">
  
        <ul class="navbar-nav">
            @ChildContent
        </ul>
</div>

@code{

    [CascadingParameter(Name = "Navbar")] Navbar Navbar { get; set; }

}